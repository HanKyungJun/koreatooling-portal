# cnc-wiki — Claude/Cowork 공용 규약 (CLAUDE.md)

> 이 파일은 Claude Code와 Cowork 모두 자동으로 읽는 규약서입니다.
> 위키 스키마, 세션 프로토콜, 출처 인용 표준, 정보 분류 원칙을 정의합니다.
> 마지막 갱신: 2026-07-28

---

## 0. 세션 시작 프로토콜 (필수)

**모든 작업 전에 다음 파일을 순서대로 읽습니다:**

1. `wiki/_handoff/context.md` — 프로젝트 큰 그림·범위·용어
2. `wiki/_handoff/decisions.md` — 최신 확정 결정 (최신이 맨 위)
3. `wiki/_handoff/tasks.md` — 현재 작업 큐 (P0~P3 우선순위)
4. `wiki/_handoff/worklog.md` 최신 핸드오프 블록 — 이전 세션이 남긴 상태

파일 간 충돌 시 `decisions.md` 최신 항목이 항상 우선합니다.

## 0-B. 세션 종료 프로토콜 (필수)

세션 종료 전 `wiki/_handoff/worklog.md` **맨 위**에 핸드오프 블록을 추가합니다:

```markdown
## YYYY-MM-DD (#N) 핸드오프 — [Cowork|Code|챗]
- 한 일: ...
- 결과/산출물: ... (파일 경로 포함)
- 미완·블로커: ...
- 다음 단계 제안: ...
- 챗으로 가져갈 질문: ...
```

세션 종료 시 아래 PowerShell commit 명령도 함께 제공합니다 (한경준님이 직접 실행):

```powershell
cd C:\Users\TOOLKOREA\Desktop\cnc-wiki
git add .
git commit -m "[이번 세션 작업 요약 — Claude가 작성]"
```

> ⚠️ **Cowork bash에서 git 명령은 동작하지 않습니다** (Windows 마운트 폴더 제약, 실측 확인).
> 파일 편집은 Edit/Write 도구로, commit은 반드시 한경준님이 PowerShell에서 실행합니다.

---

## 1. 프로젝트 구조

```
cnc-wiki/
├── CLAUDE.md                  ← 이 파일 (규약서)
├── AGENTS.md                  ← Codex 협업 규약 (별도)
├── SECURITY_NOTES.md          ← 보안 주의사항
├── .env.example               ← 환경변수 템플릿
├── .gitignore
│
├── raw/                       ← 원본 소스 (읽기 전용, LLM 수정 금지)
│   ├── Catalog/               ← 공구·휠 카탈로그 PDF (JJ, COGO, 홍익다이아, 이화다이아)
│   ├── tool/                  ← ANCA .tom 파일 (공구 프로그램 원본, 165개)
│   ├── wheel/                 ← ANCA .wpk 파일 (휠팩 원본, 33개)
│   ├── manuals/               ← 장비 매뉴얼
│   ├── articles/              ← 기사·블로그·포럼 글
│   ├── notes/                 ← 현장 메모·가공 일지·작업자 작성 xlsx
│   ├── codes/                 ← G-code 파일·매크로·스크립트
│   └── assets/                ← 이미지·도면 스크린샷
│
├── wiki/
│   ├── index.md               ← 전체 페이지 카탈로그 (항상 최신 유지)
│   ├── log.md                 ← 위키 변경 이력 (시간순, 추가 전용)
│   ├── overview.md            ← 위키 현황 요약
│   ├── _handoff/              ← 환경 간 컨텍스트 공유 (아래 §0 참조)
│   │   ├── README.md
│   │   ├── context.md
│   │   ├── decisions.md
│   │   ├── worklog.md
│   │   ├── tasks.md
│   │   ├── memory.md
│   │   ├── sources.md
│   │   ├── holidays.md        ← 휴무일·휴가 단일 소스 (§13 참조)
│   │   └── chat-project-instructions.md
│   ├── machines/              ← 장비 (ANCA, VOLLMER, Rollomatic 등)
│   ├── materials/             ← 소재 (초경, S45C, SUS304, 알루미늄, 공구코팅)
│   ├── tools/                 ← 공구·연삭 조건·코어 직경·수명·표면조도
│   │   └── wheels/            ← 연삭 휠 (보유 5종 + 카탈로그 7페이지)
│   ├── cadcam/                ← CAD/CAM·ANCA e-Sharp 시리즈·연삭 테스트 방법
│   ├── gcode/                 ← G-code 레퍼런스 (G83 등)
│   ├── comparisons/           ← 비교 분석 (3사 비교, KPI, 휠 비교)
│   ├── reports/               ← xlsx 보고서·체크리스트 (ANCA 마스터, 코어 권장 등)
│   ├── scripts/               ← 자동화 스크립트 문서 (17개 카탈로그)
│   ├── projects/              ← 프로젝트별 기록 (공구사양 실험 이력 등)
│   ├── troubleshoot/          ← 장애·트러블슈팅
│   └── erp/                   ← ERP(이카운트) 사용법·업무 프로세스 (2026-07-27 신규 — §6-10 참조)
│
├── outputs/                   ← 스크립트 실행 결과 (임시, .gitignore 대상 가능)
├── scripts/                   ← 자동화 Python 스크립트 (parse_anca.py 등)
└── dist/                      ← 정적 웹 화면 (빌드 산출물)
```

---

## 2. 핵심 원칙

1. **원본 소스는 절대 수정하지 않는다** — `raw/`는 읽기 전용
2. **가공 조건은 항상 출처를 밝힌다** — `sources.md` ID 형식 사용 (아래 §3 참조)
3. **모순이 발견되면 삭제하지 말고 병기한다** — "매뉴얼에는 3000RPM이나 실제로는 2500RPM이 안정적"
4. **숫자에는 단위를 반드시 표기한다** — RPM, mm/min, mm, Ra, m/s, mm³/(mm·s) 등
5. **안전 관련 주의사항은 눈에 띄게 표시한다** — `⚠️ 주의:` 접두사 사용
6. **검증되지 않은 조건을 확정값처럼 기록하지 않는다** — 아래 신뢰도 4단계 표기 사용
7. **민감정보·토큰·단가는 위키에 두지 않는다** — 아래 §4 정보 분류 원칙 참조
8. **조건 수정 시 기존 값을 삭제하지 않는다** — 변경 이력(날짜·사유·양산 적용 여부)을 남긴다
9. **위키는 재현 가능한 제조 기록 시스템이다** — 동일 조건에서 동일 결과를 재현할 수 있도록 작성
10. **테스트 중인 G코드 파일은 조건 변경 시 같은 파일명에 덮어쓰지 않는다** (2026-07-28 등재) — `raw/report/` 등에서 반복 수정되는 G코드(예: O0003.txt)는 개정 때마다 실행본을 `{원본명}_{차수}차_실행본_YYYYMMDD.txt`처럼 버전별 파일명으로 남긴다. 같은 파일에 계속 덮어쓰면 실제 가공에 사용된 과거 버전이 git 이력을 뒤져야만 복원 가능해지고, 위키의 "조건 변경 이력" 표와 실제 파일이 어긋나게 된다 (2026-07-28, AL엔드밀 2차 G코드 유실·git 복원 사례로 확인 — [[projects/AL엔드밀-홈가공-테스트-2]] 참고).

### 데이터 신뢰도 4단계

모든 수치·조건에는 아래 중 하나로 신뢰도를 명시합니다:

| 레벨 | 의미 |
|------|------|
| **제조사 기준** | 제조사 카탈로그·매뉴얼 직접 인용 |
| **실측 검증** | 사내 테스트로 재현 확인된 값 |
| **사내 경험값** | 반복 경험에서 얻은 값, 체계적 검증 미완 |
| **추정값** | 근거가 불충분하거나 유추된 값 |

### 출처 우선순위

1순위. 제조사 카탈로그·매뉴얼 (ANCA, Norton Winter, Walter 등)
2순위. ISO·JIS·DIN·ANSI 국제 표준
3순위. 학술 논문·핸드북·교재
4순위. 사내 실측 테스트 데이터
5순위. 커뮤니티·블로그 (보조 참고만, 단독 인용 금지)

### 조건 적용 안전 규칙

- 제조사 기준·실측 검증 없이 신규 조건의 양산 적용을 권장하지 않는다
- 신규 조건은 반드시 테스트 → 측정 → 검증 단계를 거친다
- 아래 상황에서는 `⚠️ 경고:`를 표시한다:
  - 고속 연삭 (Vc > 30 m/s, Resin Bond 초과 가능성)
  - CBN 휠 고RPM 운전
  - 초경 고절입 (DOC가 권장값의 150% 초과)
  - MCT 고이송 (카탈로그 최대값 초과)

---

## 3. 출처 인용 표준 (sources.md 연동)

모든 가공 조건·수치·권장값에 출처를 밝힙니다.
`wiki/_handoff/sources.md`에 등재된 ID를 사용합니다.

### ID 체계

| 접두사 | 의미 | 예시 |
|--------|------|------|
| `[STD-XXX]` | 국제 표준 (ISO, JIS, ASTM, DIN 등) | `[STD-ISO513]` |
| `[ACA-XXX]` | 학술 논문·핸드북·교재 | `[ACA-MALKINGUO2008]` |
| `[VEN-XXX]` | 제조사 카탈로그·기술 자료 | `[VEN-NORTON-WINTER-2023]` |
| `[CAT-XXX]` | 보유 카탈로그 (raw/Catalog/) | `[CAT-HID]` |
| `[IND-XXX]` | 산업 보고서·뉴스 | `[IND-HKKE-EHWA2022]` |
| `[SYS-XXX]` | 핸드오프 시스템 근거 문헌 | `[SYS-KLEPPMANN]` |

### 프론트매터 인용 형식

```yaml
sources:
  - "[CAT-HID]"
  - "[STD-ISO12413]"
  - "[VEN-NORTON-WINTER-2023]"
```

### 본문 인용 형식

```markdown
초경 엔드밀 플루팅 권장 Q'w = 6-12 mm³/(mm·s) [VEN-ANCA-GRAF2011].
Vc 상한은 18-25 m/s [VEN-NORTON-WINTER-2023], 초경+Resin Diamond ANCA 실측 case study 기준.
```

새 출처가 등장하면 `wiki/_handoff/sources.md`에 먼저 ID를 등재한 후 본문에서 참조합니다.

---

## 4. 정보 분류 원칙 (2026-05-11 합의)

| 분류 | 항목 | 처리 |
|------|------|------|
| **일반 데이터** | 연삭 조건·코팅 종류·수명·마모 패턴·운영 노하우 | 위키에 직접 기록 |
| **익명화 권장** | 거래처별 데이터·특정 고객 조건 | "거래처 A/B/C" 표기 |
| **진짜 대외비** | **실거래 단가·금액** (할인율 적용 후 단가, 거래처별 단가, 견적 금액) | `wiki/_private/` 또는 항목명만 위키에 |
| **개인정보** | 직원명·연락처·주민번호 | 위키에 두지 않음 |

> "대외비" 마커는 단가 관련 항목에만 사용합니다.
> 운영 노하우·코팅 비율·외주 업체명은 일반 데이터입니다.

### 4-1. 정가표는 대외비가 아니다 (2026-08-25 명확화)

**정가표(공표 정가)는 대외 공개용 자료이므로 대외비가 아닙니다** — 한경준님 확인(2026-08-25). 파일명에 "단가"가 들어간다고 일괄 대외비로 판단하지 않습니다.

| 구분 | 성격 | 예 |
|---|---|---|
| **정가표** | 대외 공개용 — **대외비 아님** | `raw/단가표/26.09 재연마정가표_정리.xlsx` |
| **실거래 단가·할인율** | **대외비** — 정가에 할인율을 적용한 값(`ROUNDUP(정가×할인율, -1)`) | 거래처별 적용 단가 |
| **견적 금액** | **대외비** | ANCA 견적서 금액, 서비스 요율, 부품비 |

> 판단 기준: **"고객이 볼 수 있는 값인가"** — 정가는 공개, 그 고객에게만 적용되는 값은 대외비.

---

## 5. 의사결정 기록 형식 (decisions.md ADR)

작업 중 새로운 결정이 발생하면 `wiki/_handoff/decisions.md` **맨 위**에 추가합니다:

```markdown
## YYYY-MM-DD: <한 줄 요약>
- 결정: 무엇을 할 것인가
- 근거: 왜 그렇게 결정했는가 (검토한 대안 포함)
  - 학술·표준 출처: [ID] 저자(연도). 제목. 식별자.
- 출처: Cowork | Code | 챗 | 현장
- 영향: 어떤 파일·작업에 영향을 주는가
```

---

## 6. 페이지 유형 및 스키마

### 6-1. 공통 프론트매터

모든 위키 페이지는 YAML 프론트매터를 포함합니다:

```yaml
---
type: machine | material | tool | wheel | gcode | cadcam | troubleshoot | project | comparison | script | report
tags: [관련 태그들]
sources:
  - "[ID-1]"
  - "[ID-2]"
updated: YYYY-MM-DD
---
```

`sources`는 `wiki/_handoff/sources.md`의 ID 형식을 사용합니다.

---

### 6-2. 장비 페이지 (`wiki/machines/`)

파일명: `{장비명}.md` (예: `anca-cnc-tool-grinder.md`)

```markdown
---
type: machine
manufacturer: "ANCA"
model: "MX7"
controller: "CIM"
tags: [CNC, 툴그라인더, ANCA]
sources:
  - "[VEN-ANCA]"
updated: YYYY-MM-DD
---

# ANCA MX7 CNC Tool Grinder

## 회사 개요
## 시리즈 라인업
## 주요 사양
## 소프트웨어 생태계
## 사내 운영 현황
## 관련 페이지
```

---

### 6-3. 소재 페이지 (`wiki/materials/`)

파일명: `{소재명}.md` (예: `sus304.md`, `알루미늄.md`)

신뢰도 등급을 3단계로 표시합니다:
- `★★★` — 학술·ISO 표준 1차 출처 직접 인용
- `★★☆` — 복수 산업 카탈로그 또는 핸드북 간접 확인
- `★☆☆` — 단일 출처 또는 경험적 추정

```markdown
---
type: material
category: "스테인리스강"
iso_group: "M"
hardness_range: "HRB 70-90"
tags: [SUS, 스테인리스, 난삭재]
sources:
  - "[STD-ISO15510]"
  - "[ACA-DAVIM2014-SS]"
updated: YYYY-MM-DD
---

# SUS304 (AISI 304 / 1.4301 / X5CrNi18-10)

## 1. 규격·명칭 (★★★)
## 2. 화학 조성 (★★★)
## 3. 기계적 물성 (★★★)
## 4. 가공 특성 (★★☆)
## 5. 추천 가공 조건 (★★☆)
## 6. 코팅 선택 (★★★)
## 7. 절삭유 (★★☆)
## 8. 사내 데이터 [대외비: 단가 제외]
## 9. 트러블슈팅
## 10. 관련 페이지
## 11. 참고 문헌
```

---

### 6-4. 공구 페이지 (`wiki/tools/`)

파일명: `{공구유형-규격}.md` (예: `공구-코어-직경.md`, `공구-코팅.md`)

```markdown
---
type: tool
category: "코어 직경"
tags: [코어, 직경, 설계, 강성]
sources:
  - "[STD-ISO3002-1]"
  - "[ACA-ENGIN2001]"
updated: YYYY-MM-DD
---
```

---

### 6-5. 연삭 휠 페이지 (`wiki/tools/wheels/`)

파일명: `휠-{각도}-Ø{직경}-{스택번호}.md`

```markdown
---
type: wheel
bond: "Resin"
abrasive: "Diamond"
shape_code: "11V9"
diameter_mm: 125
angle_deg: 20
stack: "1-1"
tags: [휠, Diamond, Resin, HID]
sources:
  - "[CAT-HID]"
  - "[STD-ISO12413]"
  - "[VEN-NORTON-WINTER-2023]"
updated: YYYY-MM-DD
---

# 휠 20도 Ø125 (스택 1-1)

## 1. 사양 (★★☆ — 카탈로그 추정)
## 2. 용도·사용 공구
## 3. 원주속도 운영 정책 (★★★)
  - Vc 권장 범위: 18-25 m/s [VEN-NORTON-WINTER-2023]
  - 장비 최대 RPM 한계: ... rpm
## 4. 카탈로그 매칭 (추정)
## 5. 드레싱 변수
## 6. 사내 측정 데이터 [ANCA 사내 적용 체크리스트]
## 7. 관련 페이지
```

---

### 6-6. G-code 레퍼런스 (`wiki/gcode/`)

파일명: `{코드}.md` (예: `g83-드릴사이클.md`)

```markdown
---
type: gcode
controller: "FANUC"
category: "고정 사이클"
tags: [G83, 드릴, 펙드릴]
sources:
  - "[STD-ISO6983]"
  - "[VEN-FANUC-0iMF]"
  - "[ACA-SMID2008]"
updated: YYYY-MM-DD
---
```

---

### 6-7. CAD/CAM 페이지 (`wiki/cadcam/`)

파일명: `{주제}.md` (예: `연삭-테스트-방법.md`, `휠-밸런싱-iBalance.md`)

ANCA e-Sharp 시리즈 페이지는 출처 ID `[VEN-ANCA-ESHARP-ARCHIVE]`를 기본으로 포함합니다.

---

### 6-8. 비교/분석 페이지 (`wiki/comparisons/`)

파일명: `{비교주제}.md` (예: `CNC-공구연삭-3사-비교.md`)

---

### 6-9. 트러블슈팅 (`wiki/troubleshoot/`)

파일명: `{증상}.md` (예: `표면조도-불량.md`)

해결 기록 테이블을 포함합니다:

```markdown
| 날짜 | 소재 | 상황 | 원인 | 해결 |
|------|------|------|------|------|
```

---

### 6-10. ERP·업무 프로세스 (`wiki/erp/`) (2026-07-27 신규)

파일명: `{시스템명}-사용법.md` (예: `이카운트-ERP-사용법.md`)

가공·연삭 조건이 아닌 사내 업무 시스템(ERP 등) 사용법을 다루는 페이지 카테고리. §4 정보 분류 원칙에 따라 **타 직원 실명·개인 평가 데이터는 기록하지 않음**(구조·절차만 기록).

```yaml
---
type: erp
system: "시스템명"
tags: [ERP, 업무프로세스]
sources:
  - "[ID-1]"
updated: YYYY-MM-DD
---
```

---

## 7. 교차 참조 규칙

- Obsidian 위키링크 문법 사용: `[[페이지명]]` 또는 `[[페이지명|표시 텍스트]]`
- 소재 ↔ 공구 ↔ 휠 ↔ 장비 간 양방향 링크 유지
- 모든 페이지 하단에 **"관련 페이지"** 섹션 포함
- 새 페이지 생성 시 `wiki/index.md`에 즉시 등재
- 고아 페이지(인바운드 링크 0개) 발생 시 index 재점검

---

## 8. 워크플로우

### 8-1. 신규 페이지 작성

1. `wiki/_handoff/context.md`·`decisions.md` 확인 → 방향 정합 확인
2. `wiki/_handoff/sources.md`에서 관련 출처 ID 수집 (없으면 먼저 등재)
3. 해당 카테고리 스키마(§6)에 맞춰 페이지 작성
4. `wiki/index.md` 해당 섹션에 등재
5. `wiki/log.md`에 `[신규]` 항목 추가
6. 세션 종료 시 `worklog.md` 핸드오프 블록 작성 (§0-B)

### 8-2. 기존 페이지 보강

1. 현재 파일 내용 확인 후 편집 (기존 출처 ID 유지)
2. 추가 내용에 새 출처 ID 병기
3. `updated` 날짜 갱신
4. `wiki/log.md`에 `[업데이트]` 항목 추가

### 8-3. sources.md 신규 출처 등재

1. 해당 섹션(0-A, 0-B, … 또는 1~8)에 ID·APA 형식으로 추가
2. 섹션 7 "내부 위키 페이지 맵"에 페이지 ↔ 출처 ID 매핑 추가
3. 프론트매터 `sources` 필드에 ID 추가

### 8-4. 검수 (Lint)

정기 확인 항목:
- 페이지 간 가공 조건 모순 (같은 소재+공구인데 다른 조건)
- 출처 없는 수치 (→ "추정값" 명시 또는 출처 추가)
- 고아 페이지 (인바운드 링크 없음)
- 빨간 링크 (언급만 되고 페이지 없음)
- 프론트매터 누락·불완전
- sources.md ID 미등재 인용

---

## 9. 테스트·불량 기록 표준

### 9-1. 테스트 데이터 기록 항목

사내 가공·연삭 테스트 결과 기록 시 아래 항목을 포함합니다:

| 항목 | 예시 |
|------|------|
| 장비명 | ANCA MX7 |
| 휠 사양 | 11V9 Ø125 Resin Diamond D46 |
| RPM / Vc | 3,050 rpm / 20 m/s |
| Feed / Q'w | Q'w 8 mm³/(mm·s) |
| DOC / Ae | 0.3 mm |
| 쿨런트 | 수용성 에멀전 5%, 20 bar |
| 피삭재·공구 | D6 4날 초경 엔드밀 WC-Co |
| 측정 장비 | Mitutoyo SJ-210 |
| 결과 | Ra 0.4 μm, 수명 45개/드레싱 |

### 9-2. 설비 상태 기록

결과에 영향을 줄 수 있는 설비 상태를 함께 기록합니다:
- 스핀들 런아웃·열변형 여부
- 휠 밸런스 (iBalance 측정값)
- 드레싱 상태 (마지막 드레싱 시점·횟수)
- 필터 오염 상태
- 쿨런트 농도·pH
- 장비 예열 여부

### 9-3. 불량 기록 항목

불량 발생 시 아래 항목을 함께 기록합니다:
- 증상 (표면 거칠기 악화·날 끝 치핑·연삭 화상·버 발생·코어 편심·런아웃 증가)
- 발생 위치 (가공 부위·공구 형상)
- 추정 원인
- 대응 방법
- 재발 여부

### 9-4. 버전 관리 기록 형식

조건 수정 시 기존 값을 삭제하지 않고 변경 이력을 남깁니다:

```markdown
| 날짜 | 변경 내용 | 사유 | 양산 적용 |
|------|----------|------|----------|
| 2026-03 | Vc 25→20 m/s | 휠 파손 후 안전 마진 확보 | ✅ 적용 중 |
| 2026-05 | DOC 0.3→0.5 mm | 가공 시간 단축 테스트 | 🔄 검증 중 |
```

---

## 10. log.md 항목 형식

```markdown
## [YYYY-MM-DD] 신규 | 업데이트 | 검수 | 삭제 — 제목

변경 요약

영향받은 페이지: [[페이지1]], [[페이지2]]
출처 추가: [ID-1], [ID-2]
```

---

## 11. Cowork 환경 전용 주의사항

| 항목 | 내용 |
|------|------|
| 파일 편집 | `Edit`/`Write` 도구 사용 (정상 작동) |
| git commit | **PowerShell에서만** 실행 (bash 불가, 실측 확인) |
| 대용량 파일 파싱 | bash + python 스크립트 활용 |
| 네트워크 접근 | web_fetch / WebSearch 도구 사용 |
| 임시 산출물 | `outputs/` 폴더에 저장 |

---

## 12. 코멘트·피드백 작성 원칙

기존 내용을 덮어쓰기보다, 출처를 밝힌 추가 블록으로 남깁니다:

```markdown
> [작성자: Cowork / 날짜: YYYY-MM-DD]
> 의견: ...
> 근거: [ID] 저자(연도).
```

---

## 13. 휴무일·휴가 관리 (단일 소스, 2026-08-06 신설)

`wiki/_handoff/holidays.md`가 "회사 전체가 쉬는 날"의 **단일 진실 공급원**입니다. 법정공휴일·사내 하계휴가 등
회사 전체 휴무는 이 파일에만 기록하고, `daily-report-check`·`scheduler-diagnose` 스킬과 "Weekly Calendar
Overview" 아티팩트가 모두 이 파일을 참조합니다(각자 따로 하드코딩하지 않음).

**계기**: 07-17 제헌절 오탐(3일 연속), 08-03·04 하계휴가 원인 조사, Weekly Calendar Overview 아티팩트의
현충일 날짜 오류(06-05→06-06 정정)까지 — 휴무일 정보가 여러 곳에 따로 박혀 있어 반복적으로 "누락 오탐"이
발생했던 것을 계기로 신설.

**새 휴무일·휴가 공지 시 절차** (상세는 `holidays.md` §3 참조):
1. `holidays.md`에 먼저 등록
2. "Weekly Calendar Overview" 아티팩트의 `CO_EVENTS`에 반영 후 `update_artifact` (Google Calendar 커넥터
   권한 재승인 필요할 수 있음, 사용자에게 안내)
3. 결정 성격이면 `decisions.md`에 ADR, 단순 정보 반영이면 `worklog.md`에 병기

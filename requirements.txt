# cnc-wiki Python 의존 패키지
# 생성: 2026-05-29 (update_overview.py, daily_report.py, Google Drive 스크립트 등)
#
# 설치:
#   pip install -r requirements.txt
#
# Google OAuth 사용 스크립트(browse_gdrive, list_gdrive, sync_to_gdrive,
# upload_to_gdrive, upload_to_sheets)는 추가로 아래 설치 필요:
#   pip install google-auth google-auth-oauthlib google-api-python-client

# ── 엑셀·데이터 처리 ──────────────────────────────────────────────────────────
openpyxl>=3.1.0
pandas>=2.0.0
odfpy>=1.4.0          # ODS 포맷 지원 (pandas 옵션)

# ── PDF 처리 ──────────────────────────────────────────────────────────────────
pdfplumber>=0.10.0
pillow>=10.0.0         # 이미지 처리 (pdfplumber 의존)

# ── Google API (Drive / Sheets OAuth) ────────────────────────────────────────
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-api-python-client>=2.0.0

# ── 환경변수 ──────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── HTTP ─────────────────────────────────────────────────────────────────────
requests>=2.28.0
